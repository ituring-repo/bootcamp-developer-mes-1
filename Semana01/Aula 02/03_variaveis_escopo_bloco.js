// Antes de ES6(2015), JavaScript só tinha escopo global e de função.
// Variáveis declaradas dentro de { } bloco não podem ser acessadas fora do bloco

// Exemplo 1 (Usando var):

// Exemplo 2 (Agora com let/const):
